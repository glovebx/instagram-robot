<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <!--<ListPreference-->
        <!--android:defaultValue="5"-->
        <!--android:entries="@array/pref_contact_sync_titles"-->
        <!--android:entryValues="@array/pref_contact_sync_values"-->
        <!--android:key="contact_sync"-->
        <!--android:negativeButtonText="@null"-->
        <!--android:positiveButtonText="@null"-->
        <!--android:title="@string/pref_title_contact_sync" />-->

    <Preference android:title="全部删除"
        android:key="@string/pref_title_contact_erase"
        android:summary="删除设备上所有联系人"/>

    <Preference android:title="批量导入"
        android:key="@string/pref_title_contact_import"
        android:summary="批量导入联系人"/>
    <!-- This preference simply launches an intent when selected. Use this UI sparingly, per
         design guidelines. -->
    <!--<PreferenceCategory android:title="@string/pref_title_contact_sync">-->
        <!--<Preference android:title="Click to exit" android:key="exitlink"/>-->
    <!--</PreferenceCategory>-->

</PreferenceScreen>
